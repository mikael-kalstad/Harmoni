import React from 'react';
import styled from 'styled-components';
import { ModalFooter } from 'react-bootstrap';

const Footer = (props: any) => {
  return <ModalFooter>Test</ModalFooter>;
};

export default Footer;
